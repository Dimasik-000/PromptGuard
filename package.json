{
  "name": "promptguard",
  "version": "1.0.0",
  "private": true,
  "description": "PromptGuard model and runtime helpers for Node.js",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "download:model": "node scripts/download-model.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
